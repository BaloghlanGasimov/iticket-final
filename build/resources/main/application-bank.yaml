spring:
  datasource:
    url: jdbc:postgresql://localhost:5440/iticket_db
    username: admin
    password: ${DB_PASS}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8090